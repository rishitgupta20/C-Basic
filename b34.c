#include <stdio.h>
int main()
{
    float a,add;
    printf("enter the number in float : \n");
    scanf("%f",&a);
    add=(float)2*a;
    printf("addition of two same float numbers are = %f",add);
    return 0;
}